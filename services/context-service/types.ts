// ─── Context Service — Types ────────────────────────────────────────────────
// Re-exports the shared types from types/research.ts for service consumers.

export type {
  TaskContextJSON,
  TaskContextTask,
  TaskContextProject,
  TaskContextRelatedTask,
  TaskContextMember,
  TaskContextSprint,
  TaskContextMilestone,
} from '@/types/research'
